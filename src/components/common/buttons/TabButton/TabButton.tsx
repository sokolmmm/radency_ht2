import React from 'react'
import styles from './TabButton.module.scss'

function TabButton (): any {
  return (
    <button type="button" className={styles.tabButton}>
      Active
    </button>
  )
}

export default TabButton
